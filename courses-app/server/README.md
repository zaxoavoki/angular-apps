# API for courses application

This server is not ready for production. Don't use it anywhere. It was build for testing purposes.

## Installation

- Clone repo
- Run `npm install`
- Run `npm run start` or `npm run dev`
